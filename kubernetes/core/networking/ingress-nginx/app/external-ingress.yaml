---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: external-ingress-homeassistant
  namespace: networking
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
    external-dns.alpha.kubernetes.io/target: "ingress.${SECRET_DOMAIN}"
    external-dns.home.arpa/enabled: "true"
spec:
  ingressClassName: nginx
  rules:
    - host: &host "ha.${SECRET_DOMAIN}"
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: external-ip-homeassistant
                port:
                  number: 80
  tls:
    - hosts:
        - *host
      secretName: tls-external-homeassistant

# SECOND FOR WJP TEST
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: external-ingress-wjp-dev
  namespace: networking
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
    external-dns.alpha.kubernetes.io/target: "ingress.${SECRET_EXT_INFRA_DOMAIN}"
    external-dns.home.arpa/enabled: "true"
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
spec:
  ingressClassName: nginx
  rules:
    - host: &host "wjp.${SECRET_B1_DOMAIN}"
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: external-ip-wjp-dev
                port:
                  number: 80
          - pathType: Prefix
            path: /api
            backend:
              service:
                name: external-ip-wjp-dev-api
                port:
                  number: 80
  tls:
    - hosts:
        - *host
      secretName: tls-external-wjp-dev
