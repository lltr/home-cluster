---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ory-keto
  namespace: appsplatform-dev
spec:
  interval: 15m
  chart:
    spec:
      chart: keto
      version: 0.37.0
      sourceRef:
        kind: HelmRepository
        name: ory
        namespace: flux-system
  maxHistory: 2
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    deployment:
      extraVolumes:
        - name: config-volume
          secret:
            secretName: ory-keto-config
      extraVolumeMounts:
        - name: config-volume
          mountPath: /etc/keto
      extraArgs:
        - -c
        - /etc/keto/config.yaml
      annotations:
        reloader.stakater.com/auto: "true"

    keto:
      automigration:
        enabled: true
      development: false
    secret:
      enabled: false
      nameOverride: "ory-keto-secrets"

    ingress:
      read:
        enabled: true
        className: "nginx"
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt-production
          external-dns.alpha.kubernetes.io/target: "ingress.${SECRET_DOMAIN}"
          external-dns.home.arpa/enabled: "true"
        hosts:
          - host: &readhost keto-read.${SECRET_DEV_DOMAIN}
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *readhost
            secretName: keto-read-tls
      write:
        enabled: true
        className: "nginx"
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt-production
          external-dns.alpha.kubernetes.io/target: "ingress.${SECRET_DOMAIN}"
          external-dns.home.arpa/enabled: "true"
        hosts:
          - host: &writehost keto-write.${SECRET_DEV_DOMAIN}
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *writehost
            secretName: keto-write-tls
