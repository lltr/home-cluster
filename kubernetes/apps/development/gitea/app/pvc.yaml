apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gitea-postgres
  labels:
    app.kubernetes.io/name: gitea-postgres
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
